# Server-only URL used by Next.js Route Handlers to reach Render.
BACKEND_URL=http://localhost:3333
# Optional public display name. No secret should use NEXT_PUBLIC_.
NEXT_PUBLIC_APP_NAME=Projeto Health
